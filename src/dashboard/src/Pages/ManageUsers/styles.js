import {makeStyles} from "@material-ui/styles";

const useStyles = makeStyles(theme => ({
  root : {

  },
  title: {
    fontSize: '2rem',
    fontWeight: 500,
    marginBottom: '1rem'
  }
}));


export default useStyles;